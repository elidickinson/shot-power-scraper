[pytest]
markers =
    browser_required: marks tests as requiring a browser (deselect with '-m "not browser_required"')
    slow: marks tests as slow (deselect with '-m "not slow"')
    quick: marks tests as quick/unit tests (select with '-m quick')
addopts = -v