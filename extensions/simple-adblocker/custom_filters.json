{
  "comment": "Add your custom ad blocking rules here. Each rule should have a unique id, priority, action, and condition.",
  "custom_rules": [
    {
      "id": 100,
      "priority": 1,
      "action": { "type": "block" },
      "condition": {
        "urlFilter": "*example-ad-server.com*",
        "resourceTypes": ["script", "image", "xmlhttprequest", "sub_frame"]
      }
    }
  ]
}